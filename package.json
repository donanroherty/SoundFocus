{
  "name": "time-daddy",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-preset-react-native-stage-0": "^1.0.1",
    "jest": "^23.4.1",
    "jest-react-native": "^18.0.0",
    "react-test-renderer": "16.3.1"
  },
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "test": "jest",
    "bashStart": "bash bashstart.sh",
    "clean": "cd android && ./gradlew.bat clean",
    "cleanrun": "cd android && ./gradlew.bat clean && cd.. && react-native run-android",
    "emulator": "emulator -avd Pixel_2_XL_API_28 -partition-size 512 -feature WindowsHypervisorPlatform -dns-server 8.8.8.8"
  },
  "jest": {
    "preset": "react-native"
  },
  "dependencies": {
    "react": "16.3.1",
    "react-native": "~0.55.2"
  }
}
